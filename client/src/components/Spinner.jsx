import React from 'react';

// Le spinner est maintenant dans son propre conteneur pour un centrage parfait.
const Spinner = () => (
  <div className="spinner-container">
    <div className="spinner"></div>
  </div>
);

export default Spinner;