# API Reference (mirror)

Cette page est un miroir court.

Reference complete et maintenue:
- `wiki/API_REFERENCE.md`

Points cles actuels:
- contrat erreur standard (`error.code`, `error.message`, `error.requestId`)
- endpoints quiz server-authoritative (`/api/quiz-question`, `/api/quiz/submit`)
- endpoints reports, taxa, places, health
