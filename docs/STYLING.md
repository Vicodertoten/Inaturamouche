# Styling (status)

Le style front est defini directement dans `client/src/`.

Points techniques actuels:
- CSS par composant + styles globaux
- PWA/caching configure via `client/vite.config.js`
- lint frontend obligatoire en CI

Reference de comportement UI:
- `wiki/guides/frontend/GAME_STATE.md`
- `wiki/guides/frontend/PWA_OFFLINE.md`
